export type TextPromptArgs = {
  defaultValue?: string;
  initialValue?: string;
  placeholder?: string;
};

export type ConfirmPromptArgs = {
  positive?: string;
  negative?: string;
  initialValue?: boolean;
};
